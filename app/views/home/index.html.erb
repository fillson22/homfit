<div class='container d-flex justify-content-center'> 
  <div class="d-flex  flex-column justify-content-between align-items-center bg-white" style="width: 19rem;height: 19rem;">
    <% if user_signed_in? %>
      <h5>Что будем делать?</h5>
      <%= link_to 'Готовить', new_meal_path, class: "btn btn-primary"%>
      <%= link_to 'Новое блюдо', new_dish_path, class: "btn btn-primary"%> 
      <%= link_to 'Список блюд', dishes_path, class: "btn btn-primary"%>
      <%= link_to 'Список покупок', '#', class: "btn btn-primary"%> 
      <%= link_to 'Отчеты', meals_path, class: "btn btn-primary"%>
    <% else %>
      <%= link_to 'Войти', new_user_session_path, class: "btn btn-primary rounded-circle p-5"%>
    <% end %>
  </div>
</div>
